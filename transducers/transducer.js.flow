// @flow
//
// See flow-typed/custom.js.flow for common declarations.

declare module.exports:
  & <A, B>(transduce: transducingFn<A, B>) => transducert<A, B>
  & <A, B>(transduce: transducingFn<A, B>,
     isReady: predicate) => transducert<A, B>
  & <A, B>(transduce: transducingFn<A, B>,
     isReady: predicate,
    initialAcc: factory<*>) => transducert<A, B>;
