// @flow

'use strict';

type FN<X, Y> = (domain: X) => Y;
import transducer from './transducer';

declare var filtererToReducer: (
  filteringFn: FN<*, boolean>
) => transducer<*, *>



declare var filter: transducer<*, *>;

export default filter;

const foo = filter((x: string): string[] => x.split('\n'), 3);
